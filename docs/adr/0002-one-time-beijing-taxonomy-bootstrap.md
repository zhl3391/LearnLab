# Initialize from a Beijing taxonomy snapshot

The initial Marble-only graph will be replaced once with the Beijing Skill Taxonomy snapshot, merging its Chinese translations with upstream node structure and including Chinese-specific topics and reviewed or machine-marked prerequisite edges. Rejected edges are excluded; machine-marked edges retain review metadata for inspection. The snapshot checksum is recorded, and future source updates are intentionally manual rather than synchronized.
