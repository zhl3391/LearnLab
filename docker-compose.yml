services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: learnlab
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - learnlab-postgres:/var/lib/postgresql/data

volumes:
  learnlab-postgres:
